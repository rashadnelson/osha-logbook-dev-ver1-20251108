# Neon Postgres Database Connection
# =====================================
# DATABASE_URL is used by Prisma to connect to your Neon Postgres database
# 
# IMPORTANT: Get your connection string from Neon Dashboard (https://console.neon.tech)
# 
# For LOCAL DEVELOPMENT:
# - Use the FREE-TIER branch endpoint (not pooled connection)
# - Format: postgresql://[user]:[password]@[endpoint-id].us-east-1.aws.neon.tech/[database]?sslmode=require
# 
# For PRODUCTION/DEPLOYED ENVIRONMENTS:
# - Use the POOLED connection string for better performance
# - Format: postgresql://[user]:[password]@[endpoint-id].pooler.us-east-1.aws.neon.tech/[database]?sslmode=require
# 
# Note: Replace bracketed values with your actual Neon credentials

DATABASE_URL="postgresql://user:password@endpoint.region.aws.neon.tech/dbname?sslmode=require"

# Optional: Neon-specific environment variables
# These are typically set by deployment platforms (Vercel/Railway)
# and are NOT required for local development

# NEON_PROJECT_ID=""        # Your Neon project ID (used for API operations)
# NEON_DATABASE=""           # Database name (usually 'neondb' by default)
# NEON_BRANCH=""             # Branch name (e.g., 'main' for production, 'dev' for development)
